{
  "name": "tender-sahayak-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
"scripts": {
  "start": "node server.js",
  "test": "node --test",
  "validate:json": "node -e \"JSON.parse(require('fs').readFileSync('package.json','utf8'))\""
  }
}
