# Web app (planned)

This folder is reserved for the production web app implementation.

## Current source of truth for prototype UI

The existing prototype remains in the repository root as:

- `remixed-2208ce14.jsx`

Next implementation step is to move this prototype into a structured app (`apps/web`) and progressively wire it to backend APIs.
